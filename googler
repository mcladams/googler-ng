#!/usr/bin/env python3
import sys

# Optional: Add the current directory to sys.path so it works without pip installation
import os
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from googler_ng.ui.cli import main

if __name__ == '__main__':
    main()
